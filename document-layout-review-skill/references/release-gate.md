# 最终交付门

最终文件只有在以下条件同时满足时才可交付：

1. 最新 DOCX 的结构检查通过；
2. 模板实际使用检查通过；
3. 表格、图片和流程图检查通过；
4. 标点与空格检查通过；
5. 最新 DOCX 已重新渲染；
6. 最终页面视觉验收通过；
7. 最后一次修改发生在最后一次验收之前。

`candidate.docx` 只能是中间文件。脚本返回 PASS 也不能替代最终页面视觉验收。
